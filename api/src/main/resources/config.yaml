kumuluzee:
  name: evcharging
  version: 0.4.0
  env:
    name: dev
  datasources:
    - jndi-name: jdbc/EVChargingDS
      connection-url: jdbc:postgresql://localhost:5432/evcharging
      username: postgres
      password: postgres
